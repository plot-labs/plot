{
  "name": "plot",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev:api": "just dev-api",
    "build:api": "just build-api",
    "test:api": "just test-api",
    "dev:web": "just dev-web",
    "build:web": "just build-web",
    "lint:web": "just lint-web",
    "test": "just test",
    "build": "just build",
    "lint": "just lint"
  }
}
